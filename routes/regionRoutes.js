// import express from 'express';
// import { createRegion, getAllRegions, getRegionByDetails } from '../controllers/region.controller.js';

// const router = express.Router();

// // Route to create a new region
// router.post('/', createRegion);

// // Route to get all regions
// router.get('/', getAllRegions);

// // Route to get a region by state, district, and taluka
// router.get('/:state/:district/:taluka', getRegionByDetails);

// export default router;
